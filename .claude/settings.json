{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path'); case \"$f\" in *.cs) dotnet build \"c:/Chuck PC/GitHub/LeftOrRight/LeftOrRight/LeftOrRight.sln\" ;; esac",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
